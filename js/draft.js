grab and store the data-ipsum name
 var ipsum = $("a:focus").attr('data-ipsum');
$( "a" ).data( "ipsum" ) 



//when you type in a number and hit generate button, show the number of paragraphs by toggle .ipsum class on, and slice the paragraphs with #paragraphs
$("#submit").click(function(){
	 //store number as:
	 var paragraphs = 

	$( "p" ).slice( "paragraphs" ).toggle(  );


});

 //////on click (this) get and store data-ipsum

//$(this).focus(function() {
  //alert( "Handler for .focus() called." );
//});

.click(), 
.focus(), 
.removeClass(), 
.addClass(), 
.data(), 
.slice(), 
.find(), 
.hide(), 
.slideDown(), 
.slideUp()

when you erase the input, removeClass from paragraphs? and hide via .animate (.slideUp?


	.slideDown(function(){
		$("p").slice("paragraphs") ();